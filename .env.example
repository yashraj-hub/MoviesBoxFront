# Local dev only - points to backend running on localhost
# On Render this is not needed, /api is relative and works automatically
VITE_API_URL=http://localhost:4000/api
# Example embed base for the TV player shell
VITE_TV_PLAYER_BASE_URL=https://your-player-host/embed
